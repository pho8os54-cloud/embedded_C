1. Вывести двоичное представление целого положительного числа,
используя битовые операции (число вводится с клавиатуры).

#include <stdio.h> // представление целого положительного числа в двоичной системе.
int main()
{
    int i;
    int Positive_namber;
    printf("Введите целое положительное число: ");
    scanf("%d",&Positive_namber);
    if (Positive_namber>0)
    {
        printf("Двоичное представление числа %d:\n ",Positive_namber);
        for ( i = 31; i >= 0; i--)
        {
            if (Positive_namber&(1<<i))
                {
                    printf("1");
                }
            else
            {
                printf("0");
            }
        }
    printf("\n");
    }
    else
    {
        printf("Введите положительное число!!!\n");
    }
return 0; 
}

