2. Вывести двоичное представление целого отрицательного числа, 
используя битовые операции (число вводится с клавиатуры).

#include <stdio.h> // представление целого отрицательного числа в двоичной системе.
int main()
{
    int i;
    int Negative_namber;
    printf("Введите целое отрицательное число: ");
    scanf("%d",&Negative_namber);
    if (Negative_namber<0)
    {
        printf("Двоичное представление числа %d:\n ",Negative_namber);
        for ( i = 31; i >= 0; i--)
        {
            if (Negative_namber&(1<<i))
            {
                printf("1");
            }
            else
            {
                printf("0");
            }
        
        }
    printf("\n");
    }
        else
        {
            printf("Введите целое отрицательное число!!!\n");
        }
    
return 0; 
}
